{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "backup-selector-capability",
  "description": "Capability for the backup target selector window",
  "windows": ["backup-selector"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "window-state:default"
  ]
}
